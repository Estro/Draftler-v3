 

{{>header-full}}

<div class="row">
    <div class="login-content animated bounceIn">
                    <form  action="/resendemail/{{user}}" method="post">
                        <div class="section-title">
                            <h3>{{content.resendEmail}}</h3>
                        </div>
 						{{#messages}}
                        <div class="alert alert-success">{{.}}</div>
                        {{/messages}}
                        <div class="login-form-action clearfix">
                            <div class="pull-left">
                            <span class="checkbox-text pull-left bottom-gap">{{content.emailExpired}}</span>
                            </div>
                            <button type="submit" class="btn btn-info ">{{content.sendConfirmation}}<i class="fa fa-chevron-right"></i></button>
                        </div>
                    </form>
                </div>

			</div>

{{>footer}}

